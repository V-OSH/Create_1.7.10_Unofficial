package create;

/**
 * Main mod class stub for Create 1.7.10 Unofficial.
 *
 * <p>In Phase 0 this is a plain class — Forge {@code @Mod} annotations and
 * {@code FML*Event} handlers will be wired in Phase 1 when ForgeGradle is
 * added to the build.</p>
 */
public class CreateMod {

    public static final String MODID = "create";
    public static final String NAME = "Create 1.7.10 Unofficial";
    public static final String VERSION = "0.1.0";

    // Phase 1: @Mod annotation, preInit/init/postInit event handlers
    // Phase 1: GameRegistry block/item/tile-entity registration
    // Phase 2: recipe registration, network channel setup, event bus subscribers
}
