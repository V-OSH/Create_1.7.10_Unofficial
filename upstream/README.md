# Vendored upstream sources

`create-mc1.20.1-6.0.8/` is an unmodified source archive of the official Create tag `mc1.20.1-6.0.8`.

Source: <https://github.com/Creators-of-Create/Create/tree/mc1.20.1-6.0.8>

The snapshot is read-only project input. Do not compile, format, or edit files inside it. Ported code must be copied into the active source tree and adapted there. The upstream MIT license is preserved in the snapshot as `LICENSE.md`.
