# Archived pre-upstream prototype

This directory preserves the implementation that existed before the upstream-first reverse-port began.

- It is outside every Gradle source set and is not compiled.
- It is not an implementation reference or a source of porting status.
- Its classes, tests, and assets may be inspected only as historical project material.
- Formal work lives under `src/` in the `com.simibubi.create` package tree.

Archived on 2026-07-12 at the start of Phase A0. Existing uncommitted prototype changes were preserved during the move.
